repos:
  - repo: https://github.com/doublify/pre-commit-rust
    rev: v1.0
    hooks:
      - id: fmt
        entry: cargo +nightly fmt --manifest-path powder-bootstrap/Cargo.toml -- --config-path powder-bootstrap
        types: [rust]
